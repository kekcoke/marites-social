version: "3"
services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      DB_DATABASE_URL:
      DB_HOST:
      DB_NAME: 
      DB_PORT: 
      DB_USERNAME: 
      DB_PASSWORD: 
      DB_CONNECT_TIMEOUT:
      DB_SSL_MODE: 
      JWT_SECRET_KEY: 
      JWT_ALGORITHM: 
      JWT_EXPIRES_MINUTES:
      OAUTH_TOKEN_URL:
      LOG_LEVEL: